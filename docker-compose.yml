services:
  whitebit-mcp:
    build: .
    image: whitebit-mcp
    ports:
      - "127.0.0.1:8080:8000"
    env_file:
      - .env
    environment:
      - MCP_HOST=0.0.0.0
    restart: unless-stopped
