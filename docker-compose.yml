services:
  whitebit-mcp:
    build: .
    image: whitebit-mcp
    ports:
      - "8080:8000"
    environment:
      WHITEBIT_BASE_URL: ${WHITEBIT_BASE_URL:-https://whitebit.com}
    restart: unless-stopped
